# Staging Environment - AWS RDS PostgreSQL Database
# Use this when on the staging branch for local testing against Staging RDS
# Get these values from AWS RDS console → staging database instance

PORT=5000

DB_TYPE=postgresql
DB_HOST=dev-nyclpc-researchdatahub.ccsgwako8hnn.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=LPC-ResearchHub-Staging
DB_USER=nyclpc
DB_PASSWORD=lpcResearchDataHub1!